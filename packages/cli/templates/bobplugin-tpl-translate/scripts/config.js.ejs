/*
 * @Author: roojay
 * @Description: 打包相关的配置文件
 */

const config = {
  pkgName: '<%= tpl.name %>',
  github:{
    username: '<%= tpl.author %>',
    repository: '<%= tpl.name %>'
  }
};

module.exports = config;
